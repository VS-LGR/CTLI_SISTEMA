/**
 * Cores do PDF de certificado — reexporta a paleta institucional cinza.
 * Mantém nomes CERTIFICATE_PDF_GRAY / FORM_COLORS para imports existentes.
 */
export {
  PDF_GRAY as CERTIFICATE_PDF_GRAY,
  FORM_COLORS,
  TEXT,
} from "@/lib/institutionalPdf/theme";
